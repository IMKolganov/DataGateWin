<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="DataGateWin.Pages.AccessPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
                x:Name="TitleText"
                FontSize="20"
                FontWeight="SemiBold" />
            <Button
                x:Name="RefreshButton"
                Grid.Column="1"
                MinWidth="110"
                Click="Refresh_OnClick">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE72C;" FontSize="14" />
                    <TextBlock x:Name="RefreshButtonText" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </Grid>

        <Border
            Grid.Row="1"
            Padding="16"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="8">
            <StackPanel Spacing="8">
                <TextBlock x:Name="PlanQuotasLabel" FontWeight="SemiBold" />
                <TextBlock x:Name="PlanLineText" TextWrapping="Wrap" Opacity="0.85" />
                <TextBlock x:Name="TrafficQuotaLabel" FontWeight="SemiBold" />
                <TextBlock x:Name="QuotaMetaText" TextWrapping="Wrap" Opacity="0.85" />
                <ProgressBar
                    x:Name="QuotaBar"
                    Height="12"
                    Maximum="100"
                    Visibility="Collapsed" />
                <TextBlock x:Name="QuotaDetailsText" TextWrapping="Wrap" Opacity="0.85" />
                <TextBlock x:Name="ValidityFooterText" TextWrapping="Wrap" Opacity="0.85" />
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="12">
            <ProgressRing x:Name="LoadingRing" Width="24" Height="24" IsActive="False" />
            <TextBlock x:Name="ErrorText" Foreground="OrangeRed" />
        </StackPanel>

        <Border
            Grid.Row="3"
            Padding="0"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="8"
            BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
            BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Padding="12,10" ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="72" />
                        <ColumnDefinition Width="88" />
                        <ColumnDefinition Width="88" />
                        <ColumnDefinition Width="72" />
                        <ColumnDefinition Width="80" />
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="ColServer" FontWeight="SemiBold" />
                    <TextBlock x:Name="ColClients" Grid.Column="1" FontWeight="SemiBold" />
                    <TextBlock x:Name="ColIn" Grid.Column="2" FontWeight="SemiBold" />
                    <TextBlock x:Name="ColOut" Grid.Column="3" FontWeight="SemiBold" />
                    <TextBlock x:Name="ColOnline" Grid.Column="4" FontWeight="SemiBold" />
                    <TextBlock x:Name="ColPlan" Grid.Column="5" FontWeight="SemiBold" />
                </Grid>
                <ListView
                    x:Name="ServersList"
                    Grid.Row="1"
                    SelectionMode="None"
                    IsItemClickEnabled="False" />
            </Grid>
        </Border>

        <TextBlock x:Name="TotalClientsLineText" Grid.Row="4" Opacity="0.75" />
    </Grid>
</Page>
