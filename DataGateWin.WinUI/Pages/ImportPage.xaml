<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="DataGateWin.Pages.ImportPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="4">
            <TextBlock
                x:Name="TitleText"
                FontSize="20"
                FontWeight="SemiBold" />
            <TextBlock
                x:Name="SubtitleText"
                Opacity="0.75"
                TextWrapping="Wrap" />
        </StackPanel>

        <Border
            Grid.Row="1"
            Padding="16"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="8">
            <StackPanel Spacing="10">
                <TextBlock x:Name="ProtocolLabel" FontWeight="SemiBold" />
                <ComboBox
                    x:Name="ProtocolCombo"
                    MinWidth="280"
                    SelectionChanged="ProtocolCombo_OnSelectionChanged" />
                <TextBlock
                    x:Name="XrayHintText"
                    Foreground="{ThemeResource SystemFillColorCautionBrush}"
                    TextWrapping="Wrap"
                    Visibility="Collapsed" />
            </StackPanel>
        </Border>

        <Border
            Grid.Row="2"
            Padding="16"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="8">
            <StackPanel Spacing="10">
                <TextBlock x:Name="ImportSectionLabel" FontWeight="SemiBold" />
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <Button
                        x:Name="BrowseButton"
                        MinWidth="140"
                        Click="BrowseButton_OnClick">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE8E5;" FontSize="14" />
                            <TextBlock x:Name="BrowseButtonText" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button
                        x:Name="PasteButton"
                        MinWidth="140"
                        Click="PasteButton_OnClick">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE77F;" FontSize="14" />
                            <TextBlock x:Name="PasteButtonText" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </StackPanel>
                <TextBlock
                    x:Name="ImportHintText"
                    Opacity="0.75"
                    TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <Border
            Grid.Row="3"
            Padding="0"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock
                    x:Name="ProfilesLabel"
                    Margin="16,12,16,8"
                    FontWeight="SemiBold" />
                <ListView
                    x:Name="ProfilesList"
                    Grid.Row="1"
                    Padding="8,0,8,8"
                    SelectionMode="None" />
                <TextBlock
                    x:Name="EmptyProfilesText"
                    Grid.Row="1"
                    Margin="16"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Opacity="0.6"
                    Visibility="Collapsed" />
            </Grid>
        </Border>

        <TextBlock
            x:Name="StatusText"
            Grid.Row="4"
            Opacity="0.85"
            TextWrapping="Wrap" />
    </Grid>
</Page>
